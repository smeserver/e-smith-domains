diff -Nur -x '*.orig' -x '*.rej' e-smith-domains-1.4.0/root/usr/lib/perl5/site_perl/esmith/FormMagick/Panel/domains.pm mezzanine_patched_e-smith-domains-1.4.0/root/usr/lib/perl5/site_perl/esmith/FormMagick/Panel/domains.pm
--- e-smith-domains-1.4.0/root/usr/lib/perl5/site_perl/esmith/FormMagick/Panel/domains.pm	2007-10-20 10:16:12.000000000 -0400
+++ mezzanine_patched_e-smith-domains-1.4.0/root/usr/lib/perl5/site_perl/esmith/FormMagick/Panel/domains.pm	2007-10-20 10:15:56.000000000 -0400
@@ -311,13 +311,21 @@
         if ($forwarder2 and not $forwarder);
 
     if ($forwarder)
+    {
 	$db->set_prop('dnscache', 'Forwarder', $forwarder);
+    }
     else
+    {
 	$db->delete_prop('dnscache', 'Forwarder');
+    }
     if ($forwarder2)
+    {
 	$db->set_prop('dnscache', 'Forwarder2', $forwarder2);
+    }
     else
+    {
 	$db->delete_prop('dnscache', 'Forwarder2');
+    }
 
     unless ( system( "/sbin/e-smith/signal-event", "dns-update" ) == 0 )
     {
